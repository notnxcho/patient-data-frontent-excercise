export interface PatientType {
  id: number;
  name: string;
  createdAt: string;
  website: string;
  avatar: string;
  description: string;
}

export interface IconProps {
  color?: string;
  stroke?: number;
  size?: number;
}
